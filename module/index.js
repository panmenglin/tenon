import { initGlobalState } from './global-state';
import { TenonContainer } from './container';
const globalState = initGlobalState();
export { TenonContainer, globalState };
export default {
    TenonContainer,
    globalState,
};
